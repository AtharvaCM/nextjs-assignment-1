import type { NextPage } from "next";

// custom components
import Layout from "@/components/layout";

const Home: NextPage = () => {
  return (
    <div className="text-red-500">
      <Layout />
    </div>
  );
};

export default Home;
